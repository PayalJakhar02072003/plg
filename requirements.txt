# Streamlit Cloud (and many hosts) only run `pip install -r requirements.txt`
# from the REPOSITORY ROOT. This file pulls in the real app dependencies.
-r semantic-plagiarism-detection/requirements.txt
